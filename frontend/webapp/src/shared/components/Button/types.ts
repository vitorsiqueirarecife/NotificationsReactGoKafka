import {
    DisplayProps,
    FlexboxProps,
    PaddingProps,
    BorderProps,
    PositionProps,
    WidthProps,
    ColorProps,
  } from 'styled-system';
  
  export interface ButtonProps extends DisplayProps,
      FlexboxProps,
      PaddingProps,
      BorderProps,
      PositionProps,
      WidthProps, 
      ColorProps {}